# This project uses pyproject.toml as the single source of truth for dependencies.
# To install dependencies, use a tool like pip, pipx, or poetry with pyproject.toml.
# This file is intentionally left blank and will be removed in future cleanups.
